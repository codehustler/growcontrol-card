{
  "name": "growcontrol-card",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GrowControl - custom Lovelace card wrapping the Grow Box Manager React app",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
